PORT=3200

# Any key starting with "dummy" keeps the deterministic offline model.
OPENAI_API_KEY=dummy-openai-key
OPENAI_MODEL=gpt-4.1-mini

# Leave empty for in-memory stores; set a file path to keep conversations and runs.
SQLITE_PATH=
